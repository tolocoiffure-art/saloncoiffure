---
import Shell from '~/tenants/maison-cortes/ui/Shell.astro';
import { maisonCortesCopy, resolveMaisonCortesLocale } from '~/tenants/maison-cortes/copy';

const locale = resolveMaisonCortesLocale(Astro.cookies.get('mc-lang')?.value);
const copy = maisonCortesCopy.locales[locale];

const metadata = {
  title: `MAISON CORTES — ${copy.contactPageTitle}`,
  description: copy.contactPageDescription,
  ignoreTitleTemplate: true,
};
---

<Shell metadata={metadata} title={copy.contactPageTitle}>
  <section class="mc-section">
    <a href={`mailto:${maisonCortesCopy.contactEmail}`}>{maisonCortesCopy.contactEmail}</a>
    <p class="mc-footnote">{copy.shippingLine}</p>
  </section>
</Shell>

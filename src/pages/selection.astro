---
// Redirect helper to ensure /selection resolves to the Maison Cortes selection page.
export const prerender = false;

const target = '/maison-cortes/selection';
return Astro.redirect(target, 308);
---
